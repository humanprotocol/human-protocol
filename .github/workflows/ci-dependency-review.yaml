name: Dependency Review
on:
  pull_request:
    branches:
      - develop
      - main

permissions:
  contents: read

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: Dependency Review
        uses: actions/dependency-review-action@v5.0.0
        with:
          show-openssf-scorecard: false
          allow-ghsas:
            - GHSA-qwww-vcr4-c8h2 # we don't use vulnerable functionality
