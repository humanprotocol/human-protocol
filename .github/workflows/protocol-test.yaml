name: Protocol Test

on: [pull_request]

jobs:
  tests:
    name: Protocol Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: borales/actions-yarn@v3.0.0
        with:
          node-version: 16
          cmd: install
      - uses: borales/actions-yarn@v3.0.0
        with:
          cmd: protocol:test
