import { FC } from 'react';
import { Launchpad as LaunchpadContainer, PageWrapper } from 'src/components';

export const Launchpad: FC = () => {
  return (
    <PageWrapper>
      <LaunchpadContainer />
    </PageWrapper>
  );
};
