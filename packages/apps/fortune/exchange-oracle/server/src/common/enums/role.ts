export enum AuthSignatureRole {
  JobLauncher = 'job_launcher',
  Recording = 'recording',
  Reputation = 'reputation',
  Worker = 'worker',
}

export enum Role {
  Worker = 'worker',
  HumanApp = 'human_app',
}
