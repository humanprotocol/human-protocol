export enum CredentialStatus {
  ACTIVE = 'active',
  EXPIRED = 'expired',
  VALIDATED = 'validated',
  ON_CHAIN = 'on_chain',
}

export enum CredentialValidationStatus {
  VALIDATED = 'validated',
  ON_CHAIN = 'on_chain',
}
