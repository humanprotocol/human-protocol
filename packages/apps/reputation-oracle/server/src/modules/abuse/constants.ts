export enum AbuseStatus {
  PENDING = 'pending',
  COMPLETED = 'completed',
  FAILED = 'failed',
  NOTIFIED = 'notified',
}

export enum AbuseDecision {
  REJECTED = 'rejected',
  ACCEPTED = 'accepted',
}
