{
  "private": "true",
  "name": "@human-protocol/fortune-v2",
  "version": "1.0.0",
  "description": "Human Protocol Fortune V2",
  "license": "MIT",
  "scripts": {
    "job-launcher-server:dev": "cd job-launcher/server && yarn start",
    "local": "docker compose down && (concurrently --hide 0 \"docker compose up -d\" \"yarn job-launcher-server:dev\")",
    "lint": "echo \"lint\"",
    "lint:fix": "echo \"lint:fix\"",
    "pretest": "yarn workspace @human-protocol/sdk build",
    "test": "echo \"pass\""
  },
  "devDependencies": {
    "@human-protocol/core": "*",
    "axios": "^1.1.3",
    "hardhat": "^2.12.2",
    "web3": "^1.8.0"
  }
}
