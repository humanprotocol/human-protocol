{
    "installCommand": "yarn install && yarn workspace @human-protocol/core compile",
    "buildCommand": "yarn workspace @human-protocol/fortune-reputation-oracle build",
    "rewrites": [{ "source": "/(.*)", "destination": "/" }]
  }
